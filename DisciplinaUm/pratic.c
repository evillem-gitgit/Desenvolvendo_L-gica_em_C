#include <stdio.h>//biblioteca padrao de entrada e saida

#include <stdlib.h>
//system("pause"); so da pra usar com essa biblioteca ai acima


int main() {
/*
    int idade;
    char nome[255];
    char sexo;
    float altura;
    double peso;



// ATENÇÃO:
// SE VC TIVER UM scanf ANTES DE UM fgets,
//


    printf("Digita tua idade: ");
    scanf("%d", &idade);
    printf("Sua idade: %d\n", idade);

    while (getchar() != '\n'); //limpa o buffer do teclado

    printf("Digita teu nome: ");
    fgets(nome, sizeof(nome), stdin);//stdin = teclado
    printf("Seu Nome: %s", nome);

    printf("\nDigita teu sexo (M/F): ");
    scanf(" %c", &sexo);
    printf("Seu sexo: %c", sexo);

    printf("\nDigita tua altura (em metros): ");
    scanf("%f", &altura);
    printf("Sua altura: %.2f metros", altura);

    printf("\nDigita teu peso (em kg): ");
    scanf("%lf", &peso);
    printf("Seu peso: %.3lf kg", peso);


    if(peso > 80.0 && altura < 1.70){
        printf("\nVoce esta acima do peso ideal!\n");
    } else {
        printf("\nVoce esta dentro do peso ideal!\n");

    }


*/
///////////////////////////////////////////////////////////////////////////////////////////////////
/*




    float preco = 10;
    float preco2 = 20;

    if (preco < preco2) {
        printf("O preco %.2f eh menor que o preco %.2f\n", preco, preco2);
    } else if (preco > preco2) {
        printf("O preco %.2f eh maior que o preco %.2f\n", preco, preco2);
    } else {
        printf("Os precos sao iguais: %.2f e %.2f\n", preco, preco2);
    }






*/
///////////////////////////////////////////////////////////////////////////////////////////////////

/*
    float temperatura, umidade;
    unsigned int estoque, estoque_minimo = 10;

    printf("\nDigite a temperatura: ");
    scanf("%f", &temperatura);

    printf("Digite a umidade: ");
    scanf("%f", &umidade);

    printf("Digite o estoque: ");
    scanf("%u", &estoque);

    if (temperatura > 30.0) {
        printf("Temperatura esta alta!\n");
    } else {
        printf("Temperatura esta adequada!\n");
    }

    if(umidade > 50){
        printf("Umidade esta alta!\n");
    } else {
        printf("Umidade esta adequada!\n");
    }

    if(estoque > estoque_minimo){
        printf("Estoque OK!: %d\n", estoque);
    } else {
        printf("Estoque abaixo do esperado!: %d\n", estoque);
    }

*/



// DA FACULDADE
//Desafio: nível novato

// ATENÇÃO:
// SE VC TIVER UM scanf ANTES DE UM fgets


}